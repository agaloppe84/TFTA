<div class="text-center">
  <h1>Editer votre Reservation</h1>
  <%= render 'formulaire' %>
  <p><%= link_to "INDEX", reservations_path, class: "btn btn-primary" %></p>
  <p><%= link_to "SHOW", reservation_path(@reservation), class: "btn btn-primary" %></p>
</div>
